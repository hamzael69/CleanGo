{% extends 'base.html.twig' %}

{% block title %}Connexion à votre espace CleanGo - Service de nettoyage professionnel{% endblock %}



{% block body %}
    <!-- Navigation breadcrumb -->
    {# <nav class="fixed top-0 left-0 right-0 z-40 bg-black/30 backdrop-blur-sm py-2" aria-label="Fil d'ariane">
        <div class="container mx-auto px-4">
            <div class="flex items-center space-x-2 text-white/90">
                <a href="{{ path('app_home') }}" class="hover:text-white focus:text-white focus:outline-none focus:ring-2 focus:ring-white/50 rounded-sm px-1 py-0.5 transition-colors">
                    Accueil
                </a>
                <span class="text-white/60" aria-hidden="true">›</span>
                <span class="text-white" aria-current="page">Connexion</span>
            </div>
        </div>
    </nav> #}

    {# Ajout de meta descriptions spécifiques #}
    <div class="sr-only">
        <h1>Connexion à votre espace client CleanGo</h1>
        <p>Accédez à votre espace personnel CleanGo pour gérer vos services de nettoyage et réservations</p>
    </div>

    <div class="fixed inset-0 w-full h-full -z-10" role="img" aria-label="Arrière-plan décoratif montrant un intérieur propre">
        <img src="{{ asset('img/rawe.jpg') }}" 
            alt="Intérieur moderne et propre représentant la qualité de service CleanGo" 
            class="w-full h-full object-cover object-center"
            loading="eager"
        >
        <div class="absolute inset-0 bg-black/40" aria-hidden="true"></div>
    </div>

    <main class="relative min-h-screen flex flex-col items-center justify-center w-full px-4 pt-24 pb-12" role="main">
        <section class="w-full max-w-lg bg-white/95 backdrop-blur-sm rounded-xl shadow-2xl p-6 sm:p-8 md:p-10" aria-labelledby="login-title">
            <header class="mb-8 text-center">
                <h2 id="login-title" class="text-2xl sm:text-3xl font-bold text-gray-800 mb-2">Bienvenue sur CleanGo</h2>
                <p class="text-gray-600 text-sm sm:text-base">Connectez-vous à votre compte</p>
            </header>

            <!-- Messages d'erreur -->
            {% if error %}
                <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg mb-6 flex items-start" role="alert" aria-live="polite">
                    <svg class="h-5 w-5 text-red-400 mr-2 mt-0.5 flex-shrink-0" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd" />
                    </svg>
                    <span>{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                </div>
            {% endif %}

            <!-- Message si déjà connecté -->
            {% if app.user %}
                <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg mb-6 flex items-start" role="status" aria-live="polite">
                    <svg class="h-5 w-5 text-green-400 mr-2 mt-0.5 flex-shrink-0" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.236 4.53L8.53 10.53a.75.75 0 00-1.06 1.061l2.03 2.03a.75.75 0 001.137-.089l3.857-5.481z" clip-rule="evenodd" />
                    </svg>
                    <div>
                        <p class="font-medium">Vous êtes déjà connecté</p>
                        <p class="text-sm mt-1">Connecté en tant que {{ app.user.userIdentifier }}. <a href="{{ path('app_logout') }}" class="underline hover:no-underline">Se déconnecter</a></p>
                    </div>
                </div>
            {% endif %}

            <!-- Information sécurité -->
            <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                            <path fill-rule="evenodd" d="M10 1a4.5 4.5 0 00-4.5 4.5V9H5a2 2 0 00-2 2v6a2 2 0 002 2h10a2 2 0 002-2v-6a2 2 0 00-2-2h-.5V5.5A4.5 4.5 0 0010 1zm3 8V5.5a3 3 0 10-6 0V9h6z" clip-rule="evenodd" />
                        </svg>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-sm font-medium text-blue-800">Connexion sécurisée</h3>
                        <div class="mt-2 text-sm text-blue-700">
                            <p>Vos informations de connexion sont chiffrées et protégées. Nous ne stockons jamais votre mot de passe en clair.</p>
                        </div>
                    </div>
                </div>
            </div>

            <form method="post" class="space-y-6" role="form" aria-describedby="form-description" novalidate>
                <!-- Description du formulaire pour l'accessibilité -->
                <div id="form-description" class="sr-only">
                    Formulaire de connexion CleanGo. Saisissez votre email et mot de passe pour accéder à votre espace client.
                </div>

                <fieldset class="space-y-6">
                    <legend class="sr-only">Informations de connexion</legend>
                    
                    <div>
                        <label for="username" class="block text-sm font-medium text-gray-700 mb-2">Adresse email *</label>
                        <div class="mt-1 relative group">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <img src="{{ asset('img/email.png') }}" 
                                    alt=""
                                    class="h-5 w-5 opacity-40 grayscale group-focus-within:grayscale-0 group-focus-within:opacity-100 group-focus-within:[filter:brightness(100%)_sepia(100%)_saturate(2000%)_hue-rotate(130deg)] transition-all duration-200"
                                >
                            </div>
                            <input type="email" 
                                value="{{ last_username }}" 
                                name="_username" 
                                id="username" 
                                class="block w-full pl-10 pr-3 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-[#3AA89E]/20 focus:border-[#3AA89E] bg-white/80 transition-all duration-200 text-gray-900 placeholder-gray-400" 
                                autocomplete="email" 
                                required 
                                autofocus
                                placeholder="votre@email.com"
                                aria-required="true"
                                aria-describedby="username-help"
                            >
                        </div>
                        <p id="username-help" class="mt-1 text-xs text-gray-500">
                            L'adresse email utilisée lors de votre inscription
                        </p>
                    </div>

                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Mot de passe *</label>
                        <div class="mt-1 relative group">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <img src="{{ asset('img/cadenas.png') }}" 
                                    alt=""
                                    class="h-5 w-5 opacity-40 grayscale group-focus-within:grayscale-0 group-focus-within:opacity-100 group-focus-within:[filter:brightness(100%)_sepia(100%)_saturate(2000%)_hue-rotate(130deg)] transition-all duration-200"
                                >
                            </div>
                            <input type="password" 
                                name="_password" 
                                id="password" 
                                class="block w-full pl-10 pr-3 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-[#3AA89E]/20 focus:border-[#3AA89E] bg-white/80 transition-all duration-200 text-gray-900 placeholder-gray-400" 
                                autocomplete="current-password" 
                                required
                                placeholder="••••••••"
                                aria-required="true"
                                aria-describedby="password-help"
                            >
                        </div>
                        <p id="password-help" class="mt-1 text-xs text-gray-500">
                            Votre mot de passe personnel et confidentiel
                        </p>
                    </div>
                </fieldset>

                <!-- Token CSRF -->
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                <!-- Options de connexion -->
                <div class="flex items-center justify-between text-sm">
                    <div class="flex items-center">
                        <input type="checkbox" 
                            id="remember" 
                            name="_remember_me" 
                            class="h-4 w-4 text-[#3AA89E] focus:ring-[#3AA89E] focus:ring-2 border-gray-300 rounded"
                            aria-describedby="remember-help"
                        >
                        <label for="remember" class="ml-2 text-gray-700">Se souvenir de moi</label>
                    </div>
                    <a href="#" class="text-[#3AA89E] hover:text-[#2C8278] focus:outline-none focus:ring-2 focus:ring-[#3AA89E] rounded-sm transition-colors">
                        Mot de passe oublié ?
                    </a>
                </div>
                <p id="remember-help" class="text-xs text-gray-500 mt-1">
                    Rester connecté sur cet appareil (non recommandé sur un ordinateur partagé)
                </p>

                <!-- Bouton de connexion -->
                <button type="submit" class="w-full flex justify-center items-center py-3 px-4 border border-transparent rounded-xl text-base font-medium text-white bg-[#3AA89E] hover:bg-[#2C8278] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#3AA89E] transition-all duration-200 shadow-lg shadow-[#3AA89E]/20 hover:shadow-xl hover:shadow-[#3AA89E]/30 disabled:opacity-50 disabled:cursor-not-allowed">
                    <svg class="h-5 w-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
                    </svg>
                    <span>Se connecter</span>
                </button>
            </form>

            <!-- Navigation et aide -->
            <div class="mt-8 space-y-4">
                <nav class="text-center text-sm text-gray-600" aria-label="Navigation alternative">
                    <p>
                        Vous n'avez pas de compte ?
                        <a href="{{ path('app_register') }}" 
                           class="font-medium text-[#3AA89E] hover:text-[#2C8278] focus:outline-none focus:ring-2 focus:ring-[#3AA89E] rounded-sm transition-colors"
                           rel="nofollow">
                            Créer un compte
                        </a>
                    </p>
                </nav>

                <!-- Informations RGPD et aide -->
                <div class="border-t border-gray-200 pt-4">
                    <div class="bg-gray-50 rounded-lg p-4">
                        <h3 class="text-sm font-medium text-gray-900 mb-2 flex items-center">
                            <svg class="h-4 w-4 mr-2 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                            </svg>
                            Protection de vos données
                        </h3>
                        <div class="text-xs text-gray-600 space-y-2">
                            <p>Vos données de connexion sont protégées selon les normes RGPD. Nous ne partageons jamais vos informations personnelles.</p>
                            <div class="flex flex-wrap gap-3 mt-3 pt-2 border-t border-gray-200">
                                <a href="#" class="text-[#3AA89E] hover:text-[#2C8278] focus:outline-none focus:ring-2 focus:ring-[#3AA89E] rounded-sm transition-colors">
                                    Politique de confidentialité
                                </a>
                                <a href="#" class="text-[#3AA89E] hover:text-[#2C8278] focus:outline-none focus:ring-2 focus:ring-[#3AA89E] rounded-sm transition-colors">
                                    Support client
                                </a>
                                <a href="#" class="text-[#3AA89E] hover:text-[#2C8278] focus:outline-none focus:ring-2 focus:ring-[#3AA89E] rounded-sm transition-colors">
                                    Contact
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
{% endblock %}
